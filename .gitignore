# ── Lovable‑UI source: track code, but ignore its deps & build output
lovable-ui/node_modules/
lovable-ui/dist/

# ── Backend caches
__pycache__/
*.py[cod]

# ── Repo‑root Node & global build dirs
node_modules/
build/
dist/

# ── We WANT to track frontend/ (built output), so don’t ignore it

# ── Git metadata
.git
.github/

# ── OS files
.DS_Store
